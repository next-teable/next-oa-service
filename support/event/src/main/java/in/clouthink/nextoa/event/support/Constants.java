package in.clouthink.nextoa.event.support;

public interface Constants {

    String NEXTOA_PREFIX = "in.clouthink.nextoa";
    String EMAIL_PREFIX = NEXTOA_PREFIX + ".email";
    String EMAIL_SMTP_PREFIX = EMAIL_PREFIX + ".smtp";
    String EMAIL_SENDER_PREFIX = EMAIL_PREFIX + ".sender";
    String SMS_PREFIX = NEXTOA_PREFIX + ".sms";

}
