spring:
  application:
    name: nextoa

logging:
  file:
    name: ${LOG_FILE_NAME:nextoa.log}
    path: ${LOG_FILE_PATH:/var/logging}
    max-size: ${LOG_FILE_MAX_SIZE:10}
    max-history: ${LOG_FILE_MAX_HISTORY:10}
  level:
    root: ${LOG_LEVEL:INFO}
    in.clouthink: ${LOG_LEVEL_APP:DEBUG}
