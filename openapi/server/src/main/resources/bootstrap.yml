spring:
  application:
    name: nextoa

logging:
  file: ${LOG_FILE:nextoa.log}
    max-size: ${LOG_MAX_FILE_SIZE:10}
  level:
    root: ${LOG_LEVEL:INFO}
    in.clouthink: ${LOG_LEVEL_APP:DEBUG}
  maxFileSize: ${LOG_MAX_FILE_SIZE:10}
  totalSizeCap: ${LOG_TOTAL_SIZE_CAP:100}
